import { HoverBackgroundElementDirective } from './hover-background-element.directive';

describe('HoverBackgroundElementDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverBackgroundElementDirective();
    expect(directive).toBeTruthy();
  });
});
